/**
 * Logger
 */
class Logger {
    /**
     * constructor
     * @param {string} folder
     * @param {file} file
     */
    constructor(folder, file) {
    }

    log(result) {
    }
}

// Export.
export default Logger;
